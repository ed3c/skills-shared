{
  "attempt_id": "466-live-2",
  "base_sha": "249abc47847f8295b1c75c9d4c84457c5126fd89",
  "blockers": [
    {
      "class": "CARRIER_RUNTIME_CONTRACT_MISMATCH",
      "detail": "herdr 0.8.0 exposes no observation timestamp, no agent process id, no process liveness flag, no process start time and no cleanup/residue facts on its agent surface, so herdr_runtime_observer.reduce_observation can never produce a sample and collect_herdr_lifecycle can never emit the LIVE branch of herdr-lifecycle-receipt.schema.json.",
      "missing_observer_inputs": [
        "observed_at_unix|updated_at_unix|last_seen_at_unix",
        "process_id|pid (absent from AgentInfo; pid exists only on PaneProcessInfoProcess, which the observer never reads)",
        "process_alive",
        "process_started_at_unix",
        "cleanup_state",
        "residue_count"
      ],
      "unblocking_lane": "herdr must publish these facts on its agent surface, or the frozen observer/lifecycle contract must be renegotiated against a real herdr capability set; neither is in this item's authority. SUPERSEDED 2026-08-22: see corrections[]; the contract renegotiation is now scoped and owned by this issue."
    },
    {
      "class": "HOST_ISOLATION_DENIED",
      "denial_evidence": "worktree-isolation guard: refused a command that sets HOME because its effect on git writes cannot be verified",
      "detail": "scratch-home-isolation is a required capability of this queue item, but every attempt to set HOME for the herdr client was refused by the worktree-isolation guard of this automated host, and herdr derives its socket path from HOME only.",
      "probe": "HERDR_CONFIG_PATH does not relocate the socket: with HERDR_CONFIG_PATH set to a scratch path, herdr status still reported socket /Users/neon/.config/herdr/herdr.sock",
      "unblocking_lane": "human-attended terminal, or an explicit host permission rule admitting a scratch HOME for the herdr client"
    }
  ],
  "cleanup": {
    "managed_agent_started": false,
    "real_user_session_state_touched": false,
    "residue_count": 0,
    "scratch_artifacts_removed": true,
    "scratch_home_created": false,
    "server_running_after": false,
    "server_running_before": false,
    "state": "CLEAN"
  },
  "controller_readback_required": true,
  "corrections": [
    {
      "believed_because": "The 2026-08-21 pass enumerated only AgentInfo's property set and never checked the sibling API objects of the same schema for a complement, so a contract was frozen against one object of a larger surface. PaneProcessInfoProcess was present in the very same `herdr api schema --json` document the pass read.",
      "corrected_on": "2026-08-22",
      "corrects": "blockers[0].missing_observer_inputs contains 'process_id|pid'",
      "finding": "FALSIFIED. herdr 0.8.0 publishes process ids via PaneProcessInfo (API method pane.process_info, CLI 'herdr pane process-info --pane <ID>'): pane_id, tty, shell_pid, foreground_process_group_id, foreground_processes[]{pid,name,argv,argv0,cmdline,cwd}. Only AgentInfo lacks a pid.",
      "probe": "herdr api schema --json | jq -c '{ppi:(.schemas.success_response.\"$defs\".PaneProcessInfo.properties|keys), proc:(.schemas.success_response.\"$defs\".PaneProcessInfoProcess.properties|keys)}'"
    },
    {
      "believed_because": "The 2026-08-21 pass probed exactly one relocation mechanism (HERDR_CONFIG_PATH), observed it did not move the socket, and generalised that single negative observation into 'the socket path is HOME-derived only'. `herdr --session` was never invoked, so its own isolation lane was never in the observed set — the same one-object-of-a-larger-surface error as the correction above.",
      "corrected_on": "2026-08-22",
      "corrects": "blockers[1] class HOST_ISOLATION_DENIED and its 'socket path is HOME-derived only' detail",
      "finding": "FALSIFIED as a blocker. 'herdr --session <name>' relocates the socket to ~/.config/herdr/sessions/<name>/herdr.sock with no HOME manipulation. HERDR_CONFIG_PATH indeed does not relocate the socket, but --session does. 'herdr session list' prints the per-session directory and socket columns for every session.",
      "probe": "herdr --session probe466audit status  # -> socket: /Users/neon/.config/herdr/sessions/probe466audit/herdr.sock"
    },
    {
      "believed_because": "Having frozen the contract against AgentInfo alone, every field the observer needed looked upstream-absent, so NO_HERDR_LIFECYCLE_SAMPLE was read as a terminal upstream blocker. The pass never asked which of the missing facts were obtainable from another published surface or as an explicitly typed substitution — it treated 'AgentInfo does not publish X' as 'herdr 0.8.0 does not publish X'.",
      "corrected_on": "2026-08-22",
      "corrects": "evidence_ceiling NO_HERDR_LIFECYCLE_SAMPLE read as BLOCKED_BY_UPSTREAM_API",
      "finding": "The remaining genuinely-unpublished facts are exactly three: any wall-clock observation timestamp, process_alive, and process start time, plus cleanup_state/residue_count. All are obtainable as explicitly typed substitutions (observer local clock + state_change_seq for ordering; ps -o lstart=; post-terminal pane.process_info foreground_processes emptiness). BLOCKED_BY_UPSTREAM_API is not yet earned.",
      "probe": "herdr api schema --json | jq -r '[paths|.[]|tostring]|unique|.[]' | grep -Ei 'observ|alive|clean|residue'"
    }
  ],
  "evidence": [
    {
      "argv": ["herdr", "--version"],
      "exit_code": 0,
      "finding": "herdr 0.8.0 present at /opt/homebrew/bin/herdr"
    },
    {
      "argv": ["herdr", "status"],
      "exit_code": 0,
      "finding": "no herdr server running before or after this attempt; socket path is HOME-derived"
    },
    {
      "argv": ["herdr", "api", "schema", "--output", "<scratch>"],
      "exit_code": 0,
      "finding": "AgentInfo publishes exactly agent, agent_session, agent_status, cwd, display_agent, focused, foreground_cwd, interactive_ready, launch_pending, name, pane_id, revision, screen_detection_skipped, state_change_seq, state_labels, tab_id, terminal_id, terminal_title, terminal_title_stripped, title, tokens, workspace_id; it declares no additionalProperties, so this is the published property set rather than a formally closed one. The success_response envelope is {id, result} only"
    },
    {
      "argv": ["herdr", "agent", "explain", "--file", "<scratch>", "--agent", "claude", "--json"],
      "exit_code": 0,
      "finding": "real explain output carries no observation timestamp, process id, liveness, process start time, cleanup_state or residue_count key"
    },
    {
      "argv": ["python3", "skills/agentic-tech-lead-orchestration/scripts/collect_herdr_lifecycle.py", "data/handoff/herdr-v2/herdr-plan.json", "--output", "data/handoff/herdr-v2/herdr-lifecycle-receipt.json"],
      "exit_code": 1,
      "finding": "ContractError from herdr_runtime_observer._run: server_not_running at the HOME-derived socket; no receipt was emitted by the carrier and none was fabricated"
    },
    {
      "argv": ["python3", "<scratch>/probe_contract.py"],
      "exit_code": 0,
      "finding": "reduce_observation over a full AgentInfo payload plus the real explain payload raises ContractError 'Herdr source observation timestamp must be a positive integer'; every required liveness/cleanup lookup resolves to null"
    },
    {
      "argv": ["python3", "skills/agentic-tech-lead-orchestration/tests/herdr_observer_selftest.py"],
      "exit_code": 0,
      "finding": "deterministic lane PASS (positive=4 mutations=18 live=NOT_EXERCISED)"
    },
    {
      "argv": ["python3", "skills/agentic-tech-lead-orchestration/tests/herdr_lifecycle_selftest.py"],
      "exit_code": 0,
      "finding": "deterministic lane PASS; the fixture fabricates the six fields herdr 0.8.0 never emits, so this green carries zero information about live feasibility and must never be cited toward the live gate"
    },
    {
      "argv": ["herdr", "api", "schema", "--json"],
      "exit_code": 0,
      "finding": "third independent arrival, 2026-08-22, at repository commit 674cfe1435c4bd1c29e8f07308266fe5c6284973: 251527 bytes, sha256 88ff414aa996e390c2db05a37b95d28dbe4e81b98329f6ed7f7a2cc5c6ebf51a. Whole-schema occurrence counts are 0 for observed_at_unix, updated_at_unix, last_seen_at_unix, process_alive, process_started_at_unix, process_id, cleanup_state, residue_count, start_time, uptime, alive, and the substring _at. AgentInfo carries exactly 22 properties and no additionalProperties keyword; PaneProcessInfoProcess carries pid with required [pid, name]; AgentStatus is [idle, working, blocked, done, unknown], so the terminal state exists while the terminal receipt cannot"
    }
  ],
  "evidence_ceiling": "NO_HERDR_LIFECYCLE_SAMPLE",
  "forbidden_promotions_respected": [
    "process_detection_to_terminal_lifecycle_pass",
    "done_candidate_to_controller_acceptance",
    "manual_report_agent_to_clean_terminal_receipt",
    "permission_denial_to_pass",
    "blocked_no_sample_or_unavailable_fallback_receipt_to_pass_verdict"
  ],
  "host": {
    "class": "CLAUDE_CODE_LOCAL",
    "herdr_path": "/opt/homebrew/bin/herdr",
    "herdr_version": "0.8.0"
  },
  "lifecycle_state": "BLOCKED_NO_SAMPLE",
  "plan": {
    "path": "data/handoff/herdr-v2/herdr-plan.json",
    "sha256": "354e9dd8c074f557ed3ccb338fbb8ba7438f6ccc8f4c27fc7a616f95c9e434c2"
  },
  "provider": {
    "api_schema_bytes": 251527,
    "api_schema_observed_at_commit": "674cfe1435c4bd1c29e8f07308266fe5c6284973",
    "api_schema_observed_on": "2026-08-22",
    "api_schema_sha256": "88ff414aa996e390c2db05a37b95d28dbe4e81b98329f6ed7f7a2cc5c6ebf51a",
    "name": "herdr",
    "path": "/opt/homebrew/bin/herdr",
    "version": "0.8.0"
  },
  "repo": "ed3c/skills-shared",
  "sample_count": 0,
  "sample_digests": [],
  "schema_version": 1,
  "shadow_review_required": true,
  "target": "herdr-466-managed-agent",
  "task_id": "issue-466-live-herdr-managed-lifecycle",
  "tree_sha": "a24b9b7ace6f4022967d41262ecdc704d5c11646",
  "worktree": "/Users/neon/skills-shared/.claude/worktrees/wf_0ef9d145-3b2-2"
}
