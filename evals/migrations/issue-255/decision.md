# Issue #255 staged-bundle reconciliation

Exact migration input:

```text
parallel-tech-lead-implementation.zip
d4f431ba2843d98d4bd4e9bb4ce770c1facc705263ea698897afda5fbb96c035

parallel-tech-lead-verification.md
8caef755ff8eba4d49ccab4aa41ecdc8600d9f6b17e3963d76528ca688c0340f

skills-shared base
fc8daf1a301dc293941e76cd5220664e47193880
```

The ZIP is migration input only. It is not committed as a binary, loaded at runtime, or promoted to canonical source.

## Canonical ownership

The staged standalone `parallel-tech-lead-loop` is rejected as a duplicate. The durable split is:

```text
agentic-tech-lead-orchestration
  composition/facade, contract-first fan-out, bounded repair/review/selection

dual-forge-repository-loop
  Tech Lead DAG/Worker-packet compilation and delivery/publication boundaries

git-town-stacked-pr-worker
  Stack topology, branch/worktree ancestry and synchronization laws

repo-agent-native
  grepai/source readback/SCIP/Tree-sitter/SQLite Blindspots evidence procedure

consumer repositories
  domain paths, commands, provider endpoints, credentials and live receipts
```

This is the #255 non-duplication decision. `agentic-tech-lead-orchestration` composes these owners; it does not replace them.

## Per-file result

The machine-readable sibling `bundle-reconciliation.json` binds all 23 files and their SHA-256 values:

```text
discard_duplicate  15
route_to_consumer    6
preserve_reference   2
integrate             0
rewrite               0
```

The six `bettor-arena/**` files are routed to `ed3c/bettor-arena#146`. They are not copied into the shared Skill body. All `skills-shared/parallel-tech-lead-loop/**` files are discarded as duplicate. The staged `modules/techlead/prompts.py` failure and missing `validate-adapters` CLI therefore do not become repository repair targets; importing the discarded runtime only to repair them would recreate the duplication this issue exists to prevent.

`code-graph-rag` remains retired from the active architecture. Historical and negative-control references may remain non-executable; canonical Blindspots retirement semantics stay with `repo-agent-native` / #248.

## Evidence ceiling

This decision proves only disposition of the exact staged artifact against the exact base. It does not prove live grepai, SCIP, Tree-sitter, Serena, LanceDB, Git Town, Forgejo, scheduler, Worker, publication, merge, promotion, or rollback behavior. Those remain separate exact-subject runtime lanes (#256, #231, #234 and consumer #146).

After this migration lands, the ZIP can be retired. Its digest in this record is sufficient provenance; the binary is not a repository dependency.
