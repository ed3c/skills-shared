{
  "schema_version": "skill-eval/v1",
  "id": "repo-agent-degraded-provider-fallback",
  "skill": "repo-agent-native",
  "kind": "recovery",
  "split": "dev",
  "source": {
    "kind": "manual",
    "ref": "ed3c/skills-shared#86"
  },
  "implementation_targets": [
    {
      "path": "skills/repo-agent-native/SKILL.md",
      "anchor": "## S3 — Retrieve"
    }
  ],
  "claims": [
    "degrades-without-optional-providers",
    "preserves-provider-evidence-ceilings"
  ],
  "task": {
    "prompt": "Analyze the fixture repository while semantic retrieval, symbol operations, and graph providers are unavailable. Continue through deterministic file listing, exact search, and direct source read; record each unavailable provider, its evidence ceiling, and the fallback used without treating unavailability as success.",
    "expected_artifacts": [
      "artifacts/repo-analysis.json"
    ]
  },
  "routing": {
    "should_invoke": [
      "repo-agent-native"
    ],
    "must_not_invoke": [
      "external-verify"
    ]
  },
  "conditions": [
    "no_skill",
    "current_skill",
    "candidate_skill",
    "wrong_skill"
  ],
  "runtime": {
    "fresh_workspace": true,
    "retries": 0,
    "timeout_seconds": 420,
    "network_policy": "no-network",
    "seed_count": 3
  },
  "verifier": {
    "type": "script",
    "command": "python3 evals/verifiers/verify_repo_agent_native_output.py --case repo-agent-degraded-provider-fallback",
    "outcome_assertions": [
      "at least one unavailable provider is explicit",
      "every non-PASS provider state names a fallback",
      "source claims remain source-bound",
      "provider absence is not rewritten as PASS"
    ]
  },
  "budgets": {
    "max_tool_calls": 24,
    "max_tokens": 24000,
    "max_wall_seconds": 420
  },
  "safety": {
    "forbidden": [
      "install or activate a provider",
      "invent provider health",
      "abort while deterministic source fallback remains available"
    ]
  }
}
