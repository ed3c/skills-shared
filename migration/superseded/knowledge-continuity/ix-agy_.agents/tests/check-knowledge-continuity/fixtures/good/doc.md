# 範例：沒有知識斷點的文件

## 0. 本文引用的文件

| 稱呼 | 檔案 | 它是什麼 |
|---|---|---|
| **不變量帳本** | `00-invariants.md` | 帶檔案行號的現況事實清單 |
| **黃金流文件** | `02-golden-flow.md` | 目標架構與資料流 |

## 1. 背景

依不變量帳本（`00-invariants.md`）的 NEG-101，server 不檢查既有金鑰。

現況這個覆寫沒有任何前置比對，實碼在 `account-register-v2.js:380` 的 UPDATE 那一行。

## 2. 決策

| 編號 | 內容 |
|---|---|
| INV-011 | 收到不同金鑰時必須拒絕 |

INV-011 是本文新增的，理由見 §1。

## 3. 範圍

去重後 26 列，另有 6 條新增。

狀態模型有三個階段：待啟用、解析中、已提交。完整定義在黃金流文件（`02-golden-flow.md`）。
