# Worked instance 指針

本 skill 不複製上游歷史 HTML。ix-agy 的第一個真實 worked instance 是：

`docs/plans/2026-08-06-oobe-reoobe-key-overwrite-guard/deliverables/oobe-reoobe-key-overwrite-guard.html`

它是可再生投影，來源 config 與 Markdown 位於同一 plan 目錄。需要骨架時讀該 HTML；內容事實仍以 Markdown SSOT 為準。
