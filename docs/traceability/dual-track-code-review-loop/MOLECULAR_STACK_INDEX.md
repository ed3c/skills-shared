# Dual-Track Code Review Loop — Molecular Stack index pointer

Terminal Workers under this convergence do not edit aggregate indexes. The
Molecular Stack entry for `dual-track-code-review-loop` is authored and owned
at
[`skills/git-town-stacked-pr-worker/molecular-indexes/dual-track-code-review-loop/README.md`](../../../skills/git-town-stacked-pr-worker/molecular-indexes/dual-track-code-review-loop/README.md).

This file exists only so that the traceability read order in
[`AGENTS.md`](AGENTS.md) has a same-directory stop before it hops to the
Molecular Stack owner; it does not duplicate that file's DAG, atom table or
evidence boundary. Read the Molecular Stack index itself for the current
atom table (`C0`, `D1`, `M1`, `X1`, `R1`, `R2`, `E1`, `D2`, `B1`, `L1`) and
its own evidence ceiling.

For issue-level status in this convergence's own vocabulary, see
[`ISSUE_DAG.json`](ISSUE_DAG.json). For the remaining open lanes ahead of
Local Handoff, see
[`LOCAL_HANDOFF_EXECUTION_QUEUE.json`](LOCAL_HANDOFF_EXECUTION_QUEUE.json).
