# AGENTS.md — Productization Operating Loop preflight

Owner: `ed3c/skills-shared#421`  
Preparation verifier: `#433`  
Portable implementation root: `#422`

## Mandatory read order

For any Productization Operating Loop task, read in order:

1. repository root `AGENTS.md`;
2. root `README.md`, `CONTEXT.md`, `ARCHITECTURE.md`;
3. `docs/INDEX.md`, document routing, State Machines, traceability;
4. this file;
5. [`README.md`](README.md);
6. [`implementation-preflight.json`](implementation-preflight.json);
7. [`SESSION_PROMPTS.md`](SESSION_PROMPTS.md);
8. existing composed methods: `product-reverse-engineering-loop`, `dr-to-mvp`, `truth-verify-loop`, `procedural-shadow-runtime`, `agentic-tech-lead-orchestration`, `git-town-stacked-pr-worker`, `shared-skills-infra`;
9. exact owning Issue, parent/peer Issues, PR base/head and evidence subjects.

Never reconstruct an exact subject, dependency, right, Session state, user result or payment from prior chat.

## Authority boundary

`skills-shared` owns portable procedures, schemas, deterministic compilers/evals and bootstrap method contracts. It does **not** own consumer repository state, private source URLs, model/provider sessions, GitHub merge, Google authority, legal/rights admission, customer truth or payment truth.

Kotlin Auto WebView is a consumer observation/routing carrier. It cannot become global source, work, Skill, user or commercial authority.

## Worker laws

- one active writer per branch/worktree;
- one declared path/resource lease per atom;
- Stage-1 market/user/commercial/policy lanes are siblings after `POL-C0`; do not serialize them unless an actual unmerged byte dependency exists;
- `TRUE_CHILD` requires consumed unmerged parent bytes;
- `CONVERGENCE` has exactly one shared-index/documentation owner;
- process/external-evidence dependencies never create Git ancestry;
- start dependency means readable interface; completion dependency means prerequisite receipt on its own evidence lane;
- compiler cannot qualify its own commercial/user claims;
- Shadow is findings-only and does not mutate Builder paths;
- source/policy visibility never creates rights/legal admission;
- user interest never creates payment; one payment never creates repeatability;
- prompt/dispatch request never creates `SESSION_OBSERVED`;
- bootstrap/KAW routing evidence never creates Agent/model/provider/user/paid PASS.

## Evidence vocabulary

Use repository-standard `PASS`, `FAIL`, `ABSENT`, `NOT_IMPLEMENTED`, `NOT_EXERCISED`, `SKIPPED_BY_POLICY`, `HUMAN_ADMIT_REQUIRED`. Preparation-specific readiness states in the machine file describe scheduling only and are never implementation PASS.

## Shadow Architect L3 blocks

Block promotion or dispatch on any of:

```text
consumer/shared Skill body duplication
mutable method/source identity used as completion evidence
market attention promoted to demand
feature difference promoted to switching wedge
pain promoted to willingness to switch
interest promoted to user validation
one transaction promoted to repeatable business
consumer subscription promoted to API entitlement
policy diff promoted to legal clearance
KAW/WebView access promoted to content rights
Google/CodexDoc projection promoted to canonical work truth
technical/CI/bootstrap PASS promoted to user/paid/runtime PASS
prompt packet promoted to observed Session
false TRUE_CHILD ancestry
hidden multi-writer convergence
missing rollback / evidence ceiling / negative controls
```

## Completion packet

Every Worker returns: exact input subjects, issue/branch/base/head, consumed parent bytes, changed paths, lease proof, commands/exits, positive and mutation denominator, evidence ceiling, failed/blocked/stale attempts, cleanup, rollback, next owner, and all remaining Human/external lanes.
