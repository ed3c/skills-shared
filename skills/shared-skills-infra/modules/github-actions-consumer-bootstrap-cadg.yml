# GENERATED BY CADG CONSUMER BOOTSTRAP
name: CADG PR Admission
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  cadg-bootstrap-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch pinned skills-shared
        run: |
          git clone --filter=blob:none __SHARED_REPOSITORY__ "$RUNNER_TEMP/skills-shared"
          git -C "$RUNNER_TEMP/skills-shared" checkout --detach __SHARED_COMMIT__
      - name: Verify thin CADG binding
        run: |
          python3 "$RUNNER_TEMP/skills-shared/skills/shared-skills-infra/scripts/consumer_bootstrap_cadg.py" \
            --consumer "$GITHUB_WORKSPACE" \
            --repository-id "${{ github.repository }}" \
            --check
