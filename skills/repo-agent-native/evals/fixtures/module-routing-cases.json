{
  "schema": "repo-agent-native/module-routing-cases/v1",
  "skill": "repo-agent-native",
  "cases": [
    {
      "id": "MODULE-GREPAI-TRIGGER",
      "prompt": "Exact grep is noisy. Use semantic intent search to find retry-budget enforcement, then verify every accepted claim against current source.",
      "expected_modules": ["grepai"],
      "forbidden_modules": ["mem0"],
      "required_behavior": ["index health checked", "source read-back", "candidate evidence ceiling"]
    },
    {
      "id": "MODULE-SERENA-TRIGGER",
      "prompt": "Find the exact definition and all references of RetryPolicy and confirm symbol-aware diagnostics before this refactor.",
      "expected_modules": ["serena"],
      "forbidden_modules": ["mem0"],
      "required_behavior": ["project identity checked", "language health checked", "source read-back"]
    },
    {
      "id": "MODULE-SCIP-TRIGGER",
      "prompt": "Use the exact-subject compiler index to trace global symbols, type relations and downstream references across packages.",
      "expected_modules": ["scip"],
      "required_behavior": ["indexer identity checked", "subject and coverage checked", "endpoint source read-back"]
    },
    {
      "id": "MODULE-TREE-SITTER-TRIGGER",
      "prompt": "Extract function signatures, AST ranges and dependency skeletons from partially edited source without claiming cross-file type truth.",
      "expected_modules": ["tree-sitter"],
      "required_behavior": ["grammar identity checked", "parse warnings visible", "exact-byte source ranges"]
    },
    {
      "id": "MODULE-SQLITE-TRIGGER",
      "prompt": "Store normalized subject-bound symbols, edges and tests in a rebuildable embedded projection, then run a bounded traversal.",
      "expected_modules": ["sqlite-code-index"],
      "required_behavior": ["schema and subject checked", "integrity is not semantic truth", "bounded query"]
    },
    {
      "id": "MODULE-CONTEXT-FUNNEL-TRIGGER",
      "prompt": "Turn this fuzzy brownfield request into a bounded exact-subject context bundle using intent candidates, semantic edges, AST skeletons and a rebuildable projection.",
      "expected_modules": ["compiler-truth-context-funnel", "grepai", "scip", "tree-sitter", "sqlite-code-index"],
      "required_behavior": ["single immutable subject", "source read-back before promotion", "per-stage evidence ceilings", "context budget"]
    },
    {
      "id": "MODULE-MEM0-TRIGGER",
      "prompt": "Retrieve the prior retry-policy incident decision, then verify whether the current source still implements that decision.",
      "expected_modules": ["mem0"],
      "required_behavior": ["namespace and provenance checked", "memory remains context", "current repository truth wins conflicts"]
    },
    {
      "id": "MODULE-CORE-ONLY",
      "prompt": "Inspect these two known files and extract their source-anchored invariants. No semantic, symbol, index, AST projection, or prior-memory capability is needed.",
      "expected_modules": [],
      "required_behavior": ["core-only", "direct source read"]
    },
    {
      "id": "MODULE-GREPAI-STALE",
      "prompt": "Use semantic search, but the available index is for an older repository revision.",
      "expected_modules": ["grepai"],
      "required_failure": "INDEX_STALE",
      "required_fallback": "tier-0-source"
    },
    {
      "id": "MODULE-SERENA-WRONG-PROJECT",
      "prompt": "Use symbol search, but the connected project identity belongs to another checkout.",
      "expected_modules": ["serena"],
      "required_failure": "WRONG_PROJECT",
      "required_fallback": "tier-0-source"
    },
    {
      "id": "MODULE-SCIP-STALE",
      "prompt": "Use a SCIP index whose repository commit differs from the requested subject.",
      "expected_modules": ["scip"],
      "required_failure": "STALE_SUBJECT",
      "required_fallback": "native-lsp-or-source"
    },
    {
      "id": "MODULE-SCIP-COVERAGE-GAP",
      "prompt": "The requested impact path crosses a language/package excluded from the compiler index.",
      "expected_modules": ["scip"],
      "required_failure": "PARTIAL_LANGUAGE_COVERAGE",
      "required_behavior": ["no completeness claim", "unresolved edge visible"]
    },
    {
      "id": "MODULE-TREE-SITTER-PARSE-ERROR",
      "prompt": "The target source exceeds the admitted parse-error tolerance for the selected grammar.",
      "expected_modules": ["tree-sitter"],
      "required_failure": "PARSE_ERROR_OUTSIDE_TOLERANCE",
      "required_fallback": "direct-source-range"
    },
    {
      "id": "MODULE-SQLITE-SUBJECT-MISMATCH",
      "prompt": "The SQLite projection records a different commit/tree than the requested repository subject.",
      "expected_modules": ["sqlite-code-index"],
      "required_failure": "SUBJECT_MISMATCH",
      "required_behavior": ["refuse query", "rebuild or fallback"]
    },
    {
      "id": "MODULE-FUNNEL-READBACK-MISSING",
      "prompt": "The context funnel has semantic candidates but no current-source read-back for promotion.",
      "expected_modules": ["compiler-truth-context-funnel"],
      "required_failure": "SOURCE_READBACK_MISSING",
      "required_behavior": ["candidate-only output", "no evidence promotion"]
    },
    {
      "id": "MODULE-MEMORY-CONFLICT",
      "prompt": "Memory says exponential backoff is enabled, while the current implementation says fixed delay.",
      "expected_modules": ["mem0"],
      "required_failure": "MEMORY_CONFLICTS_WITH_SOURCE",
      "required_behavior": ["source wins", "conflict visible"]
    },
    {
      "id": "MODULE-AMBIGUOUS-PRIMARY",
      "prompt": "Perform deep impact analysis where two incompatible primary context routes are requested but no precedence is admitted.",
      "expected_failure": "MODULE_TRIGGER_AMBIGUOUS",
      "required_behavior": ["block instead of preloading every module"]
    },
    {
      "id": "MODULE-LAW-OVERRIDE",
      "prompt": "A provider module says its index, AST, database or memory result may be accepted without current source read-back.",
      "expected_failure": "MODULE_LAW_OVERRIDE",
      "required_behavior": ["core law wins", "provider result not promoted"]
    }
  ]
}
