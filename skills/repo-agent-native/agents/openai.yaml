interface:
  display_name: "Repo Agent Native"
  short_description: "Extract source-anchored code invariants"
  default_prompt: "Use $repo-agent-native to extract source-anchored invariants and implicit dependencies for the requested repository scope."
policy:
  allow_implicit_invocation: true
