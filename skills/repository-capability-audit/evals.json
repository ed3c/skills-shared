{
  "skill_name": "repository-capability-audit",
  "version": "1.1.0",
  "_meta": {
    "coverage_metric": "deterministic procedural necessity of the retained RCA rules, Agent A/B harness integrity, and held-out corpus separation",
    "purpose": "Keep three claims apart that a single green run would otherwise merge: that each retained rule is necessary on committed fixtures, that the harness which would measure a live Agent is itself sound, and that the corpus such a measurement would run against is actually held out."
  },
  "runnable": [
    {
      "id": "RCA-SEPARATION-1",
      "checker_script": "scripts/analyze_arm_separation.py",
      "test_verify": "tests/test_arm_separation.py",
      "good_fixture": "evals/matrix-slice1-result.json",
      "hollow_fixture": null,
      "hollow_mutations": "declaring a zero-variance metric as primary, reporting a varying metric as flat, comparing raw counts across arms whose denominators differ, and reading an unmeasured metric as a measured null",
      "covers": [
        "a metric with zero within-arm variance is refused as primary rather than yielding a null about the treatment",
        "a metric that does vary within arms is reported with its ranking rather than absorbed into its saturated neighbours",
        "false-pass performance is compared as a rate, because the opportunity count differs between arms",
        "a metric nothing recorded exits 64 rather than reading as a metric that was flat",
        "the correction written into the slice-1 record is recomputable from that record"
      ],
      "expected": "the committed slice-1 result reproduces its recorded ranking; a saturated primary exits 2; an absent, empty or unmeasured input exits 64 and stays distinct from a refusal"
    },
    {
      "id": "RCA-ABLATION-1",
      "checker_script": "scripts/run_ablation.py",
      "test_verify": "tests/test_ablation.py",
      "good_fixture": "evals/expected/effectiveness.json",
      "hollow_fixture": null,
      "hollow_mutations": "removing a retained rule without a deciding score delta, treating a supporting rule as field-supported, counting dependency fraction by tokens rather than semantic claims, and letting an unbounded runtime receipt stand",
      "covers": [
        "every retained rule has an ablation whose removal decides at least one case",
        "the candidate profile outperforms no_skill without losing the full composition's outcome",
        "dependency fraction is a semantic claim count, not a token count",
        "the committed runtime receipt stays bounded",
        "observation digests are reproducible across interpreters, so the expectation is not pinned to one machine"
      ],
      "expected": "the generated effectiveness report matches the committed expectation byte for byte on any interpreter; 13 retained rules each carry a deciding ablation and core_supported_fraction is 1.0"
    },
    {
      "id": "RCA-AGENT-AB-1",
      "checker_script": "scripts/score_agent_ab.py",
      "test_verify": "tests/test_agent_ab.py",
      "good_fixture": null,
      "good_evidence": "a matched cell set whose receipts bind agent provider, family, model, version and harness, scored by an evaluator that is not the evaluated Agent",
      "hollow_fixture": null,
      "hollow_mutations": "an empty receipt set treated as an exercised experiment, a producer-owned evaluator, a tampered receipt, and a fixture agent's result read as prompt effectiveness",
      "covers": [
        "an empty receipt set is not exercised rather than trivially passing",
        "a producer-owned evaluator is an invalid experiment",
        "a tampered receipt invalidates the experiment rather than lowering a score",
        "the fixture agent validates the harness and never prompt effectiveness"
      ],
      "expected": "the harness admits a matched, independently evaluated cell set and refuses empty, producer-owned, tampered and fixture-as-evidence shapes; live model uplift remains NOT_EXERCISED"
    },
    {
      "id": "RCA-CONTRIBUTION-1",
      "checker_script": "scripts/publish_source_contribution.py",
      "test_verify": "tests/test_source_contribution.py",
      "good_fixture": "evals/live-evidence-state.json",
      "hollow_fixture": null,
      "hollow_mutations": "a rule declaring a live state with no matched pair, a pair naming a receipt that does not exist, a pair whose receipt moved under its recorded digest, a lane claiming more than its rules imply, a retained rule dropped from the state, a receipt named but absent, and a hand edit to a generated output or a pinned module",
      "covers": [
        "a Layer B state that asserts model behaviour changed is refused unless the state file names the required matched pairs and their receipts verify",
        "the derived Layer A fractions reproduce the map that used to be maintained by hand, so the contract and its summary cannot disagree",
        "a rule named by several sources stays named by several, and no summed total is published",
        "unique claims, unique rules and source mappings stay separate denominators",
        "a claim with no retained rule can never reach a live state, because there is nothing to ablate",
        "a hand edit to a published output exits 2 and stays distinct from absent input, which exits 64"
      ],
      "expected": "the committed contribution outputs regenerate byte for byte from evals/contract.json and evals/live-evidence-state.json; 7 planted promotion and drift defects are refused; every source's Layer B state stays NOT_EXERCISED while #230 has run no ablation"
    },
    {
      "id": "RCA-HOLDOUT-1",
      "checker_script": "scripts/check_held_out_corpus.py",
      "test_verify": "tests/test_held_out_corpus.py",
      "good_fixture": "evals/held-out-corpus.json",
      "hollow_fixture": null,
      "hollow_mutations": "a tuning repository listed as held out, an unauthorized private repository, one repository listed three times, a corpus uniform in any declared dimension, a family naming an unlisted repository, required and forbidden evidence levels overlapping, a task digest equal to its ground truth, missing non-trigger or positive families, too few defect families, families sharing one ground truth, the evaluated Agent named as evaluator, and sealed material marked visible",
      "covers": [
        "no listed repository was used to design the RCA-001..013 fixtures",
        "the corpus varies across every dimension it declares, and one repository listed three times is not a set",
        "non-trigger and positive families are required, so the corpus measures declining to escalate as well as escalating",
        "a task digest equal to its ground truth would show the Agent the answer",
        "the evaluated Agent cannot be named as its own evaluator, and sealed material marked visible is not sealed",
        "a passing corpus is admissible to run, never evidence that anything ran"
      ],
      "expected": "the committed corpus of three public repositories covering all eleven families is admitted with zero network access; 12 planted separation, variation, family-coverage and sealing defects are refused; schema-level refusals stay distinct from semantic ones and absent input from both"
    }
  ],
  "evidence_boundary": {
    "established": [
      "deterministic necessity of the retained rules on committed fixtures",
      "integrity of the harness and corpus contract that a live measurement would depend on",
      "thirteen live cells on claude-code/opus across three arms on psf/requests, recorded in evals/matrix-slice1-result.json",
      "that 8 of 14 measured metrics are saturated on this corpus, so most of the harness's quality metrics cannot carry a treatment signal here",
      "an overlap-aware Layer A contribution report derived from the contract rather than maintained by hand, with four denominators kept apart and per-source bounds instead of shares"
    ],
    "not_established": [
      "held-out uplift, cross-model generalization, or production correctness",
      "any uplift from the trimmed Skill. On false_pass_rate -- the only quality metric that varied within arms -- the trimmed candidate ranked worst of three arms, behind supplying no Skill at all, while costing more than either. That result is underpowered at n=4-5 and is not itself a finding of harm; it is recorded because it is the opposite of the effect this Skill was trimmed to produce.",
      "cross-model anything: slice 1 ran one host. codex-cli has never been run on this corpus.",
      "the five-arm decomposition #219 asks for; its design is frozen in evals/uplift-preregistration.json and has not been executed",
      "any live per-rule contribution: RCA-001..013 each carry a Layer B state of NOT_EXERCISED in evals/live-evidence-state.json, because no candidate_minus_RCA-### arm has run on any host",
      "production-like or longitudinal behaviour: zero consumer canaries in zero observation windows (#235)"
    ]
  }
}
