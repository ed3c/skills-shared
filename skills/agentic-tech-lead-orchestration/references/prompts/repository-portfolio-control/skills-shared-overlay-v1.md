# `skills-shared` Consumer Overlay v1

Read and obey `common-system-envelope.md` before this overlay.
This is a consumer-specific overlay for `ed3c/skills-shared`; it is not portable core.

At execution time:

```text
checkout        /Users/neon/skills-shared
tracking Issue  #560
base            re-resolve origin/main
runtime         CODEX_CLI_LOCAL
publication     DRAFT_FIRST
CI              ONE_FINAL_HOSTED_EPOCH_PER_CANDIDATE
```

Use subagents. Wait for all agents and consolidate their findings.

Read root and nearest AGENTS routes. Re-inventory every open Issue/PR and changed path in
one epoch. Preserve #412 as forensic source and never merge it directly while its
provenance defect remains. Do not replay its true children until a compliant replacement
parent is admitted. Treat current PR states and all SHAs in historical Issues as stale
until live readback.

Machine commits use the actual admitted local Codex identity and repository-required
`Driven-By` / `Driven-On` trailers. Merge, release, visibility, permission, provider
activation, semantic conflict, and production remain Human/repository authority.
