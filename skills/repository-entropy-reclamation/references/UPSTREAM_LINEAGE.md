# Upstream lineage and domain-decoupling record

This file records method provenance. It is not a vendored source copy, dependency lock, live upstream status, or evidence that a consumer repository was simplified safely.

## Immutable source subjects

| Source | Exact subject | Path reviewed | License | Concepts adapted |
|---|---|---|---|---|
| `Yevanchen/reclaim-code-entropy` | `491cbff12cdc6988dfb18dec15b2c3bc4db512f1` | `skills/reclaim-code-entropy/SKILL.md`, `README.md`, `README.zh.md` | MIT | contract-first survey, consumer/history/ownership proof, entropy candidate classes, one-boundary cuts, decisive verification, acceptance of an empty safe-cut set |
| `deepseek-ai/deepseek-harness` | `99f6f02fecdb7dff40c3fbc9470f5907c29f74ca` | `.agents/skills/dsh-find-simplifications/SKILL.md` | MIT | broad domain survey, trust/lifecycle ownership analysis, dependency-swap bar, note coalescing, proposal quality, PR hygiene, repository-context-first simplification |

Source URLs are intentionally represented by repository, commit, and path so the reviewed bytes remain identifiable after upstream branches move.

## Generalized procedural core

The shared Skill retains the repository-independent laws:

```text
exact subject and instructions before judgment
contract boundary classification before deletion
static tools produce candidates, not proof
production / non-production / ambiguous consumer classification
history, rationale, ownership and lifecycle analysis
protected public/persisted/dynamic/trust/accessibility/data-loss/quiescence boundaries
net concept/contract/state reduction rather than line-count optimization
one ownership boundary per cut
smallest decisive check plus broad and residue gates
no-safe-cut is a valid result
```

## Domain-specific details kept outside the core

The following stay in trigger-selected adapters or consumer repositories:

```text
DeepSeek package/service/event taxonomy
DeepSeek Agent Note lifecycle and exact note paths
Node, pnpm, knip, Cordis, ACP, MCP, UI and twin-adapter assumptions
one repository's production/non-production corpus layout
language-specific dead-code or dependency analyzers
consumer public API and durable-data policy
consumer issue/PR numbers, commands, paths, branches, receipts and current state
```

A consumer may implement an Agent Note adapter, ADR adapter, TODO adapter, or issue adapter through `modules/domain-profile.md`; none is universal.

## New closure controls added here

This repository adds controls that are not implied by either source alone:

```text
Draft 2020-12 machine contract
semantic gate for protected boundaries and evidence promotion
independent read-only Shadow verdict
Tech Lead task/global-objective composition
molecular C/K/A/E/X/D delivery relation
cloud/local/private/live/Human lane separation
Local Handoff Queue boundary
registry and exact-head CI arrival requirements
```

## Copyright and distribution boundary

Both reviewed upstream repositories declare MIT licensing. No upstream source directory or file is copied into this repository. The Skill, schema, verifier, tests, modules, and documentation here are newly authored method expressions and remain under `skills-shared`'s Apache-2.0 license. This attribution record is retained even though method ideas are not vendored code.
