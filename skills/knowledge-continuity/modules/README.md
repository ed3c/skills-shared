# `knowledge-continuity/modules/`

Modules are on-demand domain instances. They demonstrate the procedural method without redefining it.

Current module:

- [`four-repository-routing-example.md`](four-repository-routing-example.md) — worked application across `skills-shared`, `runtime-env`, a host repo, and `agent-shield-monorepo`.

Load a module only when the task matches its domain. Do not place module-specific branches, paths, or provider states into the always-loaded procedural core.
