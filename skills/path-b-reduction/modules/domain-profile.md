# Reduction-domain profile

Historical lineage, consumer examples, and actor/provider-specific details remain recoverable from pre-refactor blob `fa35735f0dfe5a1461f498813179dd0ee5699f42`.

## Trigger
Load when a concrete consumer reduction case, lineage map, actor/provider implementation, or repository-specific artifact route must be bound.

## Non-trigger
Do not load for generic observation reduction, redundancy elimination, invariant preservation, or evidence-aware compression.

## Assumptions
The input can be decomposed into required invariants, redundant material, and candidate reductions with observable acceptance criteria.

## Specialization inventory
Consumer examples, historical lineage, named actors/providers, and repository-specific reduction targets belong here.

## Evidence ceiling
A smaller artifact is not automatically better; correctness requires preserved invariants and the owning assertions.

## Fallback
If a specialized consumer/profile is unavailable, emit the generic reduction packet with preserved invariants and explicit unresolved tradeoffs.

## Forbidden overrides
This module may not override `CORE-LAW-001` through `CORE-LAW-005`, drop a required invariant for size, hide information loss, or widen provider/secret/merge authority.
