# CLAUDE.md — bettor-arena(Claude-tier 薄派生)

工程 SSOT＝`ARCHITECTURE.md`:放置契約=§2、鐵律全文=§3。規則原文只住那裡,本檔禁複述,
只留 Claude Code 專屬入口:

- 啟用:`sh bootstrap.sh`;專案 MCP(`.mcp.json`)與 hook(`.claude/settings.json`)啟用=人 admit。
- commit 前:§3 鐵律 2 的 T0 閘;落新檔前:§2 槽位對映。
- 迭代期間 root 被動上下文凍結:§3 鐵律 3。
