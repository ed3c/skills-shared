# Agentic Tech Lead golden-proof module

## Trigger

Select this module only when a refactor needs to preserve or compare the Agentic Tech Lead old monolith, refactor-as-landed, reachability repair, receipt-gated causal repair, or production-shaped deterministic real-task proof.

## Non-trigger

Do not load this module for unrelated Skills, generic body-neutrality cleanup, provider installation, live model trials, Git Town/Forgejo delivery, merge, release or production promotion.

## Frozen subjects

```text
A   OLD_MONOLITH             blob a01f53592cda98f61b413b4467afa96356fb4ef7
B0  REFACTOR_AS_LANDED       blob 8b2da7443aff7a9f53412b5af280048203bbd5e9
B1  REACHABILITY_REPAIRED    blob 51c3fd81749598957f2b993c4d31c3b4c8c277c1
B2  CAUSAL_DAG_REPAIRED      blob 3fd01571b49b1dfd1c9256661fe4aafe3ecc6e99

structural proof head          504c18f10d3380be4874a59f7cfad5c290daa93f
real-task proof head           403a4f041c5f8c07b0d7c8bb0ef2ccc44ac0f113
```

Historical blobs are immutable treatment data. Updating a branch or proof checker must not rewrite them.

## Structural proof

The deterministic rubric keeps eleven dimensions separate:

```text
A   9 / 11
B0  6 / 11
B1 10 / 11
B2 11 / 11
```

The result preserves two facts simultaneously:

- the old monolith had real strengths, including explicit T0–T10 causality and a pre-dispatch task assertion;
- B0 regressed by disconnecting semantic assertion ordering, concrete module routing and self-activation refusal.

B2 closes all tested structural/executable-contract dimensions by retaining provider neutrality and adding task-shape, task-semantic and trigger/predecessor/receipt-gated capability transitions. This is not model behavioral uplift.

## Production-shaped deterministic real-task proof

The matched canary uses one immutable temporary repository, the same base/tree, contracts, immutable oracles, budgets and deterministic Worker carrier across admitted arms. It executes:

```text
three-candidate pricing tournament
+ path-disjoint receipt Worker
+ real linked worktrees and overlapping subprocesses
+ checkpoint → retry lineage
+ failed candidate retained in denominator
+ deterministic winner
+ wrong-base convergence refusal
+ local-pass/global-fail refusal
+ verified convergence owner
+ global objective
+ zero worktree/branch/process/lease residue
```

Observed arm states:

```text
A   PASS
B0  BLOCKED_DISPATCH_ROUTE_ABSENT
B1  PASS
B2  PASS
```

The executed arms produced equivalent final bytes. B2's advantage is stronger admissibility and causal proof, not a better deterministic implementation.

## Data flow

```text
immutable treatments
→ structural A/B
→ task/schema/semantic/capability gates
→ matched worktree/subprocess canary
→ local and global oracles
→ scheduler/cleanup receipts
→ golden-proof schema
→ check_refactor_contract.py
→ scope-bounded deterministic receipt
```

## Evidence ceiling

```text
structural proof          PASS / DETERMINISTIC_FIXTURE
real-task orchestration   PASS / SYNTHETIC_RUNTIME
live model behavior       NOT_EXERCISED
live provider adapters    NOT_EXERCISED
Git Town / Forgejo        NOT_EXERCISED
publication               NOT_EXERCISED
merge                     HUMAN_ADMIT_REQUIRED
```

## Fallback

If any frozen blob, exact proof head, old strength, B0 regression, matched-arm condition, global objective, cleanup state or trace identity is absent, stop. Do not replace the missing proof with a narrative summary.

## Forbidden overrides

This module cannot change `PCR-LAW-*`, rewrite treatments, raise evidence class, suppress failed arms, claim provider/model quality, activate a provider, publish, merge, resolve a semantic conflict, release or promote.

## Trace

- causal repair: issue #307 / PR #308;
- real-task A/B: issue #312 / PR #315;
- standard: epic #326, issues #327/#328/#329;
- remaining live lanes: #231, #232, #234, #256 and #312 Phase 2.
